spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/synthesis
    username: synthesis_user
    password: changeme
  
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

security:
  jwt:
    secret: synthesis-local-secret-key-change-in-production-minimum-256-bits

aws:
  bedrock:
    region: us-east-1
    model-id: anthropic.claude-3-sonnet-20240229-v1:0
    knowledge-base-id: YOUR_KNOWLEDGE_BASE_ID_HERE
  s3:
    bucket: synthesis-attachments-local

synthesis:
  escalation:
    notification-email: dev@synthesis.local
